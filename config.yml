tunnel: media-drop-tunnel
credentials-file: /root/.cloudflared/YOUR_TUNNEL_ID.json

ingress:
  # Route traffic for media.p3lending.space to local app
  - hostname: media.p3lending.space
    service: http://127.0.0.1:8080
  
  # Catch-all for other traffic (returns 404)
  - service: http_status:404
